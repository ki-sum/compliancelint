# Dependencies
/node_modules

# Production
/build

# Generated content — populated by the upstream sync script (lives
# in the dashboard repo, sibling to this site). At deploy time, CI
# runs the sync first, then `npm run build`. We don't commit the
# generated MDX or screenshots so PR diffs stay focused on the
# scaffold + theming layer.
/docs/
/static/img/walkthrough/

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
